# خطوات التشغيل

كل الملفات جاهزة والبورتريه مولّد من صورتك. اللي عليك شيئين: ترفع الريبو، وتشغّل الـ Action أول مرة.

## ١. إنشاء الريبو ورفع الملفات

الريبو لازم يكون اسمه **بالضبط** مثل اسم المستخدم — `Taha-Alawa` — وعام (public).

```bash
cd Taha-Alawa
git init
git add .
git commit -m "profile: terminal README with animated SVG art"
gh repo create Taha-Alawa --public --source=. --push
```

أو من غير `gh`: أنشئ الريبو من واجهة GitHub باسم `Taha-Alawa`، ثم:

```bash
git remote add origin https://github.com/Taha-Alawa/Taha-Alawa.git
git branch -M main
git push -u origin main
```

افتح `github.com/Taha-Alawa` — المفروض تشوف الـ README فوق الملف الشخصي مباشرة.

## ٢. تجديد البورتريه لو غيّرت الصورة

`me-ascii.svg` مولّد فعلاً من صورتك — ما تحتاج تسوي شي. لكن إذا بدّلت الصورة:

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r scripts/requirements.txt
pip install -r scripts/requirements-portrait.txt   # ثقيلة، محلياً فقط

python scripts/prep_photo.py my-photo.jpg   # فصل الخلفية + قصّ حول الوجه
python scripts/make_ascii_svg.py            # يكتب me-ascii.svg
```

أو بأمر واحد: `./scripts/build.sh my-photo.jpg`

للمعاينة بدون حركة: `STATIC=1 python scripts/make_ascii_svg.py`

**مواصفات الصورة:** وجه مواجه للكاميرا وإضاءة فيها ظلال. السكربت يلقى الوجه تلقائياً ويقصّ حوله بنسبة ثابتة، فأبعاد الأصل ما تفرق.

جرّبنا صورة TEDx وكانت أسوأ بكثير: فصل الخلفية خلّف بقايا من الشعارات، والوجه أصغر بالنسبة للإطار فضاعت الملامح. صورة الاستديو أنظف.

**نقطة مهمة لو عدّلت الإعدادات:** البطاقة خلفيتها سوداء، يعني الرمز الكثيف (`@`) يقرأ كـ**ضوء** مو ظل. عشان كذا الصورة تُركّب على أسود لا أبيض، والتدرّج معكوس عن أغلب مولّدات ASCII: الوجه المضاء هو الطرف الكثيف، والبدلة السوداء تذوب في البطاقة. لو ركّبتها على أبيض بتطلع نيجاتيف.

الإعدادات في أعلى `make_ascii_svg.py`:

```python
LO = 48.0      # كل شي أغمق من كذا يختفي تماماً
HI = 200.0     # كل شي أفتح يصير أكثف رمز
GAMMA = 1.25   # فوق ١ يغمّق النغمات الوسطى
FLOOR = 0.055  # أخف حبر داخل القناع، يحفظ خط الشعر
```

وتحذير من تجربة: لا تشغّل CLAHE على الشبكة الصغيرة (١٠٠×٦٦) — يشبع الوجه كله في رمز واحد ويطلع كتلة صمّاء. التدرّج يجي بعد التصغير، والتصغير قبل التدرّج.

## ٣. تشغيل الأتمتة

من تبويب **Actions** في الريبو → `Update profile art` → **Run workflow**. أول تشغيل يدوي يتأكد إن البوت يقدر يكتب. بعدها بيشتغل يومياً ٦:١٧ صباحاً UTC (٩:١٧ بتوقيت السعودية) ويحدّث الهيت ماب لحاله.

إذا فشل بخطأ صلاحيات: Settings → Actions → General → Workflow permissions → اختر **Read and write permissions**.

## تعديل بطاقة المعلومات

كل المحتوى في `scripts/make_info_card.py` تحت `CONTENT` — قائمة صفوف بسيطة:

```python
("kv",     "Now", "Frontend Engineer at Matrixs Group"),
("bullet", "",    "ERP platform in Next.js: HR, CRM, projects, dashboards"),
("head",   "Shipped", ""),
("gap",    "", ""),
```

عدّل، شغّل `python scripts/make_info_card.py`، ارفع الملف.

## الرسم البياني

أربع سنوات مكدّسة، الأحدث فوق: ٢٠٢٦ / ٢٠٢٥ / ٢٠٢٤ / ٢٠٢٣. كل سنة لها صفّها وعنوانها ومجموعها على اليمين.

بعد ما فعّلت **Include private contributions**:

| السنة | قبل | بعد |
|---|---|---|
| ٢٠٢٣ | ٢٢ | ٢٢ |
| ٢٠٢٤ | ٢٤ | ٨٤٩ |
| ٢٠٢٥ | ٠ | ٢٨٣٣ |
| ٢٠٢٦ | ٣٧ | ٧٨٥ |

المجموع ٤٤٨٩ مساهمة و٥٦٢ يوم نشط.

عدد السنوات في `fetch_contributions.py`:

```python
YEARS = 4      # كل سنة = طلب واحد للصفحة العامة
```

مستويات اللون تُحسب **عبر كل السنوات مجتمعة**، مو كل سنة على حدة. عشان كذا ٢٠٢٣ تطلع باهتة و٢٠٢٥ متوهجة — وهذا مقصود؛ لو حُسبت كل سنة لوحدها بتطلع ٢٠٢٣ بنفس سطوع ٢٠٢٥ وتكذب على القارئ.

## أشياء تعلّمت أنها تكسر الشكل

- `style="..."` داخل الـ README ما له أي تأثير، GitHub يشيله. المسافات الرأسية الوحيدة اللي يحترمها هي `<br>`.
- `<h1>` و `<h2>` يرسمون خط أفقي كامل تحت العنوان. استخدمت `<h3>` عشان أتجنبه.
- ما فيه JavaScript ولا CSS خارجي — كل الحركة لازم تكون جوّه ملف الـ SVG نفسه.
- العرض في الـ README: الهيت ماب ٨٨٠ = البورتريه ٣٣٠ + البطاقة ٥٥٠، عشان الحواف تتطابق. إذا غيّرت وحدة غيّر الباقي.
